databaseChangeLog:
  - changeSet:
      author: "Parzival / Malte Linke (parzival-space)"
      id: "00-init-spring-ai-chat-memory"
      changes:
        - createTable:
            tableName: "ai_chat_memory"
            columns:
              - column:
                  name: "conversation_id"
                  type: varchar(36)
                  constraints:
                    nullable: false
              - column:
                  name: "content"
                  type: text
                  constraints:
                    nullable: false
              - column:
                  name: "type"
                  type: varchar(10)
                  constraints:
                    nullable: false
              - column:
                  name: "timestamp"
                  type: timestamp
                  defaultValueComputed: current_timestamp
                  constraints:
                    nullable: false
        - createIndex:
            indexName: ai_chat_memory_conversation_id_timestamp_idx
            tableName: ai_chat_memory
            columns:
              - column:
                  name: conversation_id
              - column:
                  name: timestamp
                  descending: true

  - changeSet:
      id: "00-init-discord-conversation"
      author: "Parzival / Malte Linke (parzival-space)"
      changes:
        - createTable:
            columns:
              - column:
                  constraints:
                    nullable: false
                    primaryKey: true
                    primaryKeyName: pk_discord_conversation
                  name: discord_id
                  type: VARCHAR(255)
              - column:
                  name: conversation_id
                  type: UUID
            tableName: discord_conversation
